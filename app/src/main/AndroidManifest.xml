<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- 일정 알람 관련 권한 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".guardian.onboarding.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".guardian.main.GuardianMainActivity" />
        <activity android:name=".guardian.onboarding.WelcomeActivity"/>
        <activity android:name=".guardian.onboarding.LoginActivity" />
        <activity android:name=".guardian.onboarding.GuardianChatActivity"/>
        <activity android:name=".guardian.onboarding.SpecialNotesActivity"/>
        <activity android:name=".guardian.onboarding.SetupCompleteActivity"/>
        <activity android:name=".guardian.schedule.ui.ScheduleAddActivity"/>

        <!-- 일정 알람 수신 BroadcastReceiver -->
        <receiver
            android:name=".user.main.schedule.service.ScheduleAlarmReceiver"
            android:exported="false" />

    </application>

</manifest>
